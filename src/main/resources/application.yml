spring:
  application:
    name: api-gateway
  messages:
    fallback-to-system-locale: false

management:
  endpoint:
    health:
      probes:
        enabled: true

logging:
  structured:
    format:
      console: ecs
